// We debated a few ideas, but ended up going with index.js because it would interact with index.md

// Declare the variable checkedOut and set it to the boolean false. This will help you know if a book has been checked out
var checkedOut = false
var genre = "Horror"

// This variable tells you how many books the library contains in this genre
var horrorBooks = 1275
// Declare the variable paperback and assign to it the value of the boolean false
var paperback = true
var authorName = "David Foster Wallace"
var pageNumber = 6

// Declare the variable "sections", and assign it the value of an array. This array contains 4 items, all of which are strings
var sections = ["Young Adult Fiction", "Science", "Sci-fi", "Fantasy"]
